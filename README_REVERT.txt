REVERT BUILD

This is the exact original codebase uploaded at the start of the session.
Push all these files to the GitHub main branch root.
Railway will auto-deploy with: python main.py
The original shipment dashboard with all tabs should be restored.
